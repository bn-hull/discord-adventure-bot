language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"

arch:
  - amd64

services:
  - docker

before_install:
  - pip install -r requirements.txt

script:
  - python -m unittest discover tests
  - python main.py
